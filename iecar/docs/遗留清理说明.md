# 遗留清理说明

## 已清理/隔离

- 交付包不保留 `__pycache__`、`.pio`、`.git`、临时日志与构建产物。
- `lables.txt` 拼写错误副本不再作为验证通过条件；K210 默认只使用 `labels.txt`，旧候选保留在脚本兼容分支内。
- legacy Task3 只在 car1 角色配置中作为旧实现标记，不进入默认任务 mask。

## 保留原因

- wrapper include `.c` 暂时保留，用于避免一次性改动链接边界造成比赛前不可控风险。
- `sbus_usart2.c` 文件名保留，但协议职责集中在 `ctrl_num_shared.c`。

## 后续删除条件

1. `pio run -e car1` 与 `pio run -e car2` 在目标主机稳定通过。
2. `pio test -e native` 覆盖协议 parser 和核心状态转换。
3. wrapper 共享实现迁移到明确的私有库或统一编译单元后，再删除薄 wrapper。
